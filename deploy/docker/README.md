# Docker Self-Hosting

The Docker deployment guide now lives in [`docs/deployment.md`](../../docs/deployment.md).

This directory still contains the deployment assets referenced there:

- `compose.yml`
- `compose.bundled.yml`
- `.env.example`
- `.env.bundled.example`
